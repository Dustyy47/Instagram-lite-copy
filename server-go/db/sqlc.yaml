version: "2"
sql:
  - engine: "postgresql"
    path: "sqlc"
    queries: "query/"
    schema: "migration/"
    
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_interface: true
